// array methods 

const months = new Array('January', 'February', 'march', 'April', 'May', 'June');

// change values of array
// months[1] = 'New Month';
// console.log(months);

// // find saomething in the array
// console.log(months.indexOf('December'));

// add more items into the array //end 
// months.push('July'); 
// console.log(months);

// // add more items into the array (at the beginning)
// months.unshift('Month 0');
// console.log(months);

// add an element in the middle
// months.splice(3, null, 'a new month'); // null means not removing any thing on index 3
// console.log(months);

// remove elements from the end
// months.pop();

//remove elements from the beginning
// months.shift();

//  remove from the middle 
// months.splice(3, 1); // remove april
// months.splice(3, 2); // remove may

console.log(months);

